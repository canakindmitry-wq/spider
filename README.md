# Симулятор Человека-паука

Браузерная HTML5-игра в жанре tycoon для **Яндекс Игр**. Игрок живёт в комнате Человека-паука: снимает фото, спасает город, улучшает костюм, камеру, блог и интерьер.

Для детей 6–12 лет: один экран без перезагрузок, только клики и касания, крупные кнопки, весь текст на русском.

## Как запустить локально

```bash
npm install
npm run dev
```

Откройте адрес Vite (по умолчанию `http://localhost:43124`).

Сборка архива для консоли Яндекс Игр:

```bash
npm run zip
```

Появится `spiderman-tycoon.zip`. Внутри — `index.html` и ассеты с относительными путями (`base: './'`). Шрифты Nunito упакованы в архив, внешних CDN нет.

Либо вручную:

```bash
npm run build
```

Папка `dist` — то же содержимое без zip.

## Как играть

Комната — твой 3D-рендер (`public/room.webp`) один в один. Поверх — отдельные спрайты предметов (`public/objects/`) и прозрачные зоны клика. При наведении увеличивается **весь** предмет, а не прямоугольный кусок комнаты. Unity не используется: для Яндекс Игр нужен HTML5-zip. Кликай по предметам на картинке:

- **Работать** (ноутбук на столе) — мини-игра фотографа: поймайте объект в рамку и нажмите куда угодно (кнопка «Снять» тоже работает).
- **Спасать** (окно) — мини-игра героя: нажимайте на жителей и злодеев, пока идёт таймер.
- **Костюм** (открытый шкаф) — прокачка костюма. После 10 уровня улучшения идут бесконечно: бонус растёт, внешний вид остаётся как на 10 уровне. Скины за монеты надеваются сразу.
- **Магазин** (коробка на ковре) — предметы для комнаты.
- **Отдыхать** (телевизор, после покупки) — бонус 200 монет раз в 7 минут.
- **Достижения** (полка с кубком) — награды за прогресс.
- **Постер с маской** — коллекция редких кадров (до 6, каждый даёт +5% к фото).
- **Камера и блог** на столе — прокачка камеры и пассивный доход. После 10 уровня бонусы растут дальше.
- **Кнопка ₽** — покупки за рубли: отключение рекламы и премиум-костюмы.

Энергии нет. Прогресс пишется в `localStorage`, а в каталоге Яндекса ещё и в облако игрока.

Полноэкранная реклама показывается автоматически каждые 5 минут в комнате (не во время фото, геройства, окон и результатов). Её можно отключить покупкой.

Музыка и эффекты — MP3 из `public/audio/`. Комната, фото и геройство кроссфейдятся. Отдельного трека «музыка геройствования» в каталоге звуков не было, поэтому в геройстве играет тот же трек, что и на фото.

## Яндекс Игры: что включить в консоли

1. Загрузите `spiderman-tycoon.zip`.
2. Включите **«Игра использует облачные сохранения»**.
3. Создайте таблицу лидеров с техническим именем **`coins`** (очки = всего заработано монет).
4. Добавьте покупки с этими ID (цены в рублях):

| ID | Товар | Цена |
| --- | --- | --- |
| `disable_ads` | Отключение рекламы | 99 ₽ |
| `skin_gold` | Золотой паук | 49 ₽ |
| `skin_knight` | Паук-рыцарь | 75 ₽ |
| `skin_cosmic` | Космический паук | 99 ₽ |
| `skin_invisible` | Паук-невидимка | 75 ₽ |
| `skin_rainbow` | Радужный паук | 49 ₽ |

Скины за монеты удешевлены вдвое (250–750 🪙).

SDK подключается как `/sdk.js` на серверах Яндекса (как требует модерация). Локально SDK не грузится: реклама и покупки идут тестовыми заглушками.

Игра вызывает `LoadingAPI.ready`, `GameplayAPI.start/stop`, полноэкранную и rewarded-рекламу, `Player.getData/setData`, `getPayments` и `getLeaderboards`.

## Стек

Чистый JavaScript (Vite), без Phaser. Комната — CSS 3D (изометрический угол из трёх плоскостей), остальная графика — CSS, SVG и эмодзи. Звук — HTMLAudioElement и MP3.
